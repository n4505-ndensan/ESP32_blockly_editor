# Blockly

`toolbox.ts`に条件・繰り返し・数値の標準ブロックを定義する。
`components/BlocklyEditor.tsx`がパネル内のワークスペースを生成・リサイズ・破棄する。
センサ値の更新でワークスペースを作り直さない。

現在は編集のみ。ブロックの実行、保存・復元は未実装。

部品のブロックは将来`GET /api/devices`のカタログから組み立てる。
部品の一覧をここに書き写すと、部品を足すたびに2か所を直すことになる。
[adr/0001](../../../adr/0001-device-state-and-control.md)を参照。
