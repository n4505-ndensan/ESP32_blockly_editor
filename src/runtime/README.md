# プログラム実行（未実装）

ASTの検証と実行エンジンの配置先。
待機・繰り返しで処理を占有せず、停止要求を処理できる構造にする。

部品を動かすときは`src/devices/`の`setDeviceOutput()`を通す。
手動操作と同じ経路にしておけば、変化が同じ`devices`イベントで画面へ届き、
実行中の状態表示を別に作らずに済む。

実行中かどうかのフラグと手動操作の排他は未決定。
決めるときは[adr/0001](../../adr/0001-device-state-and-control.md)の「将来の拡張点」を起点に、
新しいADRとして残す。
